// API endpoints
export const API_BASE_URL = "https://api.example.com";
export const LISTINGS_ENDPOINT = `${API_BASE_URL}/listings`;

// UI text
export const TEXT = {
  BOOK_NOW: "Book Now",
  VIEW_DETAILS: "View Details",
};

// Any other reusable settings
export const CONFIG = {
  MAX_LISTINGS_PER_PAGE: 20,
};
